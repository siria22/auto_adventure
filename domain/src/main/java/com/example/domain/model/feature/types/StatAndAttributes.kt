package com.example.domain.model.feature.types

enum class StatType {
    STRENGTH, AGILITY, LUCK, INTELLIGENCE
}

enum class ActorAttributeType {
    HP, ATTACK, SPEED, EVASION, MAGIC, MP, FORTUNE, ACCURACY, DEF
}